apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: question-postgres-data-pvc
  namespace: lkequiz-local
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi