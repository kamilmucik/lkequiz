apiVersion: v1
kind: Service
metadata:
  name: question-svc
  namespace: lkequiz-local
spec:
  selector:
    app: question-svc
  # type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 50055
      targetPort: 50055