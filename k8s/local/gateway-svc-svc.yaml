apiVersion: v1
kind: Service
metadata:
  name: gateway-svc
  namespace: lkequiz-local
spec:
  selector:
    app: gateway-svc
  # type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 3000
      targetPort: 3000