apiVersion: v1
kind: Service
metadata:
  name: auth-postgres
  namespace: lkequiz-local
spec:
  selector:
    app: auth-postgres
  # type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 5433
      targetPort: 5432