apiVersion: v1
kind: Service
metadata:
  name: question-postgres
  labels:
    group: db
spec:
  type: ClusterIP
  selector:
    app: question-postgres
  ports:
    - port: 5432
      targetPort: 5432